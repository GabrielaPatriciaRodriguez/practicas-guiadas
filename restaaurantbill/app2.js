const restaurantBill = parseInt(prompt("Cuanto es el total de la cuenta?")) => {
    const tax = 0.1,
    let theTotal = restaurantBill + tax, 
  
    console.log(document.write("El total de la cuenta con los impuestos es " + "$" + theTotal), /* ??? */
  };
  
  